<%= form_for @user, :html => { :multipart => true } do |f| %>
	<p>
		<%= f.fields_for :useravatar do |g|%>
		<%= g.label :useravatar %>
		<%= g.file_field :attachment %>
		<%end %>
		<%= f.submit%>
	<p>
		
<%end%>		