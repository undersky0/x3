<% @profiles.each do |u|%>
<h5>Name </h5>
<%=u.firstname %>
<%=u.lastname%>
 <br>

<% if u.user.nil?%>
<li> <%= link_to image_tag(u.user.useravatar.url(:thumb)), user_path(u.user) %> </li> 
<%else%>
no users
<%end%>
<%if Invitation.been_sent(@group, u.user)%>

	<%= link_to "#{u.user.full_name}", user_path(u.user)%> <h5>Has been Invited</h5>
	
	<%else%>
	<%= link_to "#{u.user.full_name}", user_path(u.user)%>
	<%= button_to "invite Friend", invite_invitations_path(:user_id => u.user_id, :group_id => @group.id), :action => :sendinv%>
	<%end%>

<%end%>